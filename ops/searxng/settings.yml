use_default_settings: true

general:
  debug: false
  instance_name: "Axiom SearXNG"

search:
  safe_search: 0
  formats:
    - html
    - json

server:
  port: 8080
  bind_address: "0.0.0.0"
  secret_key: "axiom-local-dev-secret-key-change-me"

ui:
  static_use_hash: true

redis:
  url: false
